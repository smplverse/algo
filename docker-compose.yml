version: "3.9"
services:
  gpu:
    build: .
    image: piotrostr/smplverse
    platform: linux/amd64
    command: python main.py
    working_dir: /code
    volumes:
      - ./:/code
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]
